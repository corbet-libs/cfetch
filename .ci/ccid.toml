schema = 1
project = "cfetch"

[checks.ci-config]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "ci-config"]]

# Profile dependencies must already exist. Native accelerator and macOS/Windows qualification remain explicit separate gates.

[checks.catalog]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "catalog"]]

[checks.rust]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "rust"]]

[checks.memory]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "memory"]]

[checks.variants]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "variants"]]

[checks.licenses]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "licenses"]]

[checks.profile]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "profile"]]

[checks.governor]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "governor"]]

[checks.ort-foundation-cpu]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "ort-foundation-cpu"]]

[checks.ort-foundation-build]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "ort-foundation-build"]]

[checks.release-guards]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "release-guards"]]

[checks.release]
kind = "commands"
commands = [["python3", "scripts/release.py"]]

[checks.maintenance]
kind = "commands"
commands = [["python3", "scripts/release_maintenance.py"]]

[checks.staging]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "staging"]]

[checks.local-model]
kind = "commands"
commands = [["bash", "scripts/ci-local-model.sh"]]

[checks.local-release]
kind = "commands"
commands = [["bash", "scripts/ci-local-release.sh"]]

[checks.nix]
kind = "commands"
commands = [["bash", "scripts/ci-nix.sh"]]

[checks.release-inputs]
kind = "commands"
commands = [["bash", "scripts/ci-release-inputs.sh"]]

[checks.embedding-candidates]
kind = "commands"
commands = [["bash", "scripts/ci-candidates.sh"]]

[checks.native-governor]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "native-governor"]]

[checks.native-worker]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "native-worker"]]

[checks.admission-origin]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "admission-origin"]]

[checks.freshness]
kind = "commands"
commands = [["bash", "scripts/ci-check.sh", "freshness"]]
